import { FilmImagePipe } from './film-image.pipe';

describe('FilmImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FilmImagePipe();
    expect(pipe).toBeTruthy();
  });
});
